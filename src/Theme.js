import { createTheme } from "@mui/material/styles";

const Theme = createTheme({
  palette: {
    primary: {
      main: "#ef7b45",
    },
    secondary: {
      main: "#d84727",
    },
    background: {
      default: "#042a2b",
    },
    text: {
      primary: "#FFF",
      secondary: "#000",
      accent: "#ef7b45",
      gray: "#adb5bd",
    },
  },
});

export default Theme;
